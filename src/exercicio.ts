// function multiplicacaoo(n1: number, n2: number): number {
//     return n1 * n2;
// }

const multiplicacao = (n1: number, n2: number): number => n1 * n2;

function saudacao(nome: string): string {
    return `Olá ${nome}`;
}

const meuNome = 'Lucas menezes';

console.log(multiplicacao(10, 10));
console.log(saudacao(meuNome));